# cdp-connector-control-service-skeleton

CDP Connector Control API 的 Spring Boot 骨架工程。  
基础包：`cn.javahome.frank.cdp`

## 运行

```bash
mvn spring-boot:run
```

## 说明

1. 该工程是接口骨架，返回示例数据，便于前后端联调。  
2. 路径与 `docs/cdp-connector-openapi.yaml` 对齐。  
3. 后续可将 Controller 中的 mock 返回替换为真实 Service 实现。
